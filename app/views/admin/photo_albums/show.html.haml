- content_for :page_title do
  Photo Album

.row
  .col.s12.offset-m1.m10
    .card-panel.main-view
      .row
        .col.s12.m6.grid
          %span.label Title:
          = @photo_album.title
        .col.s12.m6.grid
          %span.label Parent Page:
          = @photo_album.page_name
        .col.s12.m6.grid
          %span.label Order:
          = @photo_album.order
        .col.s12.m6
          %span.label Active:
          %span.icon
            - if @photo_album.active?
              = material_icon.check_box
            - else
              = material_icon.check_box_outline_blank
  .col.s12.offset-m1.m10
    - @photo_album.photos.each do |photo|
      .col.s12.m4.l2
        .card
          .card-image
            = image_tag photo.image.url


.fixed-action-btn.click-to-toggle.toolbar
  %a.btn-floating.btn-large.waves-effect.waves-light
    = material_icon.menu
  %ul
    %li.waves-effect.waves-light
      = link_to admin_photo_album_path(@photo_album), class: "red" do
        = material_icon.delete_forever
    %li.waves-effect.waves-light
      = link_to edit_admin_photo_album_path(@photo_album), class: "green" do
        = material_icon.edit
